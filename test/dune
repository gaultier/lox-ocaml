; (rule
;  (with-stdout-to hello_world.output (run ../bin/main.exe build ../../../test/hello_world.lox)))

; (alias
;  (name runtest)
;  (action (diff hello_world.expected hello_world.output)))

(tests
 (names Lox)
 (action (run %{test} build %{deps})))
